// ** Reactstrap Imports
import { Progress } from 'reactstrap'
import React from 'react'

const ProgressAnimated = ({progress}) => (
 
      <Progress animated striped className='progress-bar-success' value={progress} />

)

export default ProgressAnimated
